services:
  mysql:
    image: mysql:8.0
    container_name: promptops-mysql
    ports:
      - "3306:3306"
    volumes:
      - ./mysql/data:/var/lib/mysql
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: promptops
      MYSQL_USER: promptops
      MYSQL_PASSWORD: password
      TZ: America/Los_Angeles
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_unicode_ci

  weaviate:
    image: semitechnologies/weaviate:1.24.6
    container_name: promptops-weaviate
    ports:
      - "8080:8080"
      - "50051:50051"
    environment:
      QUERY_DEFAULTS_LIMIT: 25
      AUTHENTICATION_ANONYMOUS_ACCESS_ENABLED: "true"
      PERSISTENCE_DATA_PATH: /var/lib/weaviate
      DEFAULT_VECTORIZER_MODULE: none
      ENABLE_MODULES: ""
      CLUSTER_HOSTNAME: node1
    volumes:
      - ./weaviate/data:/var/lib/weaviate
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/v1/.well-known/ready"]
      interval: 30s
      timeout: 10s
      retries: 3

  redis:
    image: redis:7-alpine
    container_name: promptops-redis
    ports:
      - "6379:6379"
    volumes:
      - ./redis/data:/data
    command: redis-server --appendonly yes
    environment:
      TZ: America/Los_Angeles

  # MILVUS STACK (dead code — kept for rollback)
  # Milvus requires etcd + minio as dependencies.
  # To re-enable: uncomment all three services below and re-enable Milvus code in document_service.py.
  #
  # etcd:
  #   image: quay.io/coreos/etcd:v3.5.5
  #   container_name: promptops-etcd
  #   ports:
  #     - "2379:2379"
  #   environment:
  #     - ETCD_AUTO_COMPACTION_MODE=revision
  #     - ETCD_AUTO_COMPACTION_RETENTION=1000
  #     - ETCD_QUOTA_BACKEND_BYTES=4294967296
  #     - ETCD_SNAPSHOT_COUNT=50000
  #   volumes:
  #     - ./milvus/etcd:/etcd
  #   command: etcd -advertise-client-urls=http://127.0.0.1:2379 -listen-client-urls http://0.0.0.0:2379 --data-dir /etcd
  #   healthcheck:
  #     test: ["CMD", "etcdctl", "endpoint", "health"]
  #     interval: 30s
  #     timeout: 20s
  #     retries: 3
  #
  # minio:
  #   image: minio/minio:RELEASE.2023-03-20T20-16-18Z
  #   container_name: promptops-minio
  #   ports:
  #     - "9001:9001"
  #     - "9000:9000"
  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin
  #   volumes:
  #     - ./milvus/minio:/minio_data
  #   command: minio server /minio_data --console-address ":9001"
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
  #     interval: 30s
  #     timeout: 20s
  #     retries: 3
  #
  # milvus:
  #   image: milvusdb/milvus:v2.3.3
  #   container_name: promptops-milvus
  #   ports:
  #     - "19530:19530"
  #     - "9091:9091"
  #   environment:
  #     ETCD_ENDPOINTS: etcd:2379
  #     MINIO_ADDRESS: minio:9000
  #   volumes:
  #     - ./milvus/milvus:/var/lib/milvus
  #   command: milvus run standalone
  #   depends_on:
  #     - etcd
  #     - minio
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:9091/healthz"]
  #     interval: 30s
  #     start_period: 90s
  #     timeout: 20s
  #     retries: 3
